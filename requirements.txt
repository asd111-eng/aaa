# 指定Python版本约束，适配云端环境
python_version >= "3.11"

# 核心依赖（全部为兼容版本）
streamlit==1.36.0  # 降低streamlit版本，适配旧版TensorFlow
tensorflow-cpu==2.15.0  # CPU版TensorFlow，兼容所有Python 3.8-3.11
numpy==1.24.3
pillow==9.5.0
scikit-learn==1.3.2
